# 项目交接入口

在本项目开始任何开发、排查、测试或部署之前，先阅读 [PROJECT_MEMORY.md](PROJECT_MEMORY.md)。需要具体实现时，以当前代码为准；旧 README、注释和聊天记录可能已过时。

项目 Skill 位于 [.claude/skills/mini-life-game-context/SKILL.md](.claude/skills/mini-life-game-context/SKILL.md)，在维护人生护照、总控台、活动画布或部署时使用。

## 每次开发必须同步

- 修改代码、配置、数据库结构或部署状态后，在本次任务结束前更新 `PROJECT_MEMORY.md`。
- 记录实际变化、受影响文件、测试结果、部署是否发生、未完成项和下一步；中途交接也要更新。
- 保持“当前实现”部分准确，最近记录只保留有用的交接信息；历史细节以 Git 为准。
- 纯讨论且没有状态变化时，不必制造开发记录。没有执行的测试或部署，必须写“未执行/未确认”。
- 不把真实 PIN、令牌、隧道凭据、参与者联系方式或数据库内容写入这些文件。

这是协作助手的必做交接步骤，不是后台自动同步程序；不要为了更新记忆额外部署、清库或覆盖用户改动。
