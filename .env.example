# 复制成 .env 或直接设成部署平台的环境变量

# 工作人员端 PIN（各站同工用）
STAFF_PIN=换成你们自己的

# 管理员 PIN（总控台，能抽身份、改参数、导出数据、重置）
ADMIN_PIN=换成另一个只有负责人知道的

# SQLite 数据目录。部署到云上时必须指向持久卷的挂载点。
# 本地开发留空即可，默认写到 server/data/
MLG_DATA_DIR=/data

PORT=3000
